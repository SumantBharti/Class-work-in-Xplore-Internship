import pandas as pd

df = pd.DataFrame({'A': [1, None, 3], 'B': [4, 5, None]})

# Drop missing values
df1 = df.dropna()

# Fill missing values with 0
df2 = df.fillna(0)

print(df1)
print(df2)
